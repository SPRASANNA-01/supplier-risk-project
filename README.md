# Supplier Reliability & Delivery Risk
## Setup (local)
1. Clone the repo
2. Create and activate venv
```bash
python -m venv .venv
source .venv/Scripts/activate # windows: .venv\Scripts\activate
pip install -r requirements.txt
